{
  "name": "signal-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create signal-db",
    "d1:migrate": "wrangler d1 migrations apply signal-db",
    "d1:migrate:local": "wrangler d1 migrations apply signal-db --local",
    "vectorize:create": "wrangler vectorize create signal-embeddings --dimensions 1024 --metric cosine",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/zod-validator": "^0.2.0",
    "zod": "^3.22.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^3.28.0",
    "typescript": "^5.4.0"
  }
}
